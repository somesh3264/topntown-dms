# Supabase Edge Functions are Deno-only — deployed separately via the
# Supabase CLI (`supabase functions deploy`). They must NOT be included
# in the Next.js/Vercel build.
supabase/functions
supabase/.temp
