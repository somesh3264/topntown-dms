// src/types/database.types.ts
// ---------------------------------------------------------------------------
// Placeholder type file until `supabase gen types typescript` is run.
//
// To regenerate with accurate types from your live schema, run:
//   npx supabase gen types typescript --project-id YOUR_PROJECT_ID \
//     > src/types/database.types.ts
//
// Until then, Database is typed as `any` to unblock compilation.
// All Supabase client calls in the codebase will still work at runtime.
// ---------------------------------------------------------------------------

export type Database = any;
